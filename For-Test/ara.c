#include<stdio.h>
int main(){
    int x;
    printf("Enter a number (Stop enter -1): ");
    while (1)
    {
        scanf("%d",&x);
        if (x==-1)
        {
            break;
        }
}
    return 0;
}